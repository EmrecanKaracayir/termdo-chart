apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-api-deployment
  namespace: {{- .Release.Namespace -}}
spec:
  replicas: {{- .Values.gatewayApi.replicas -}}
  selector:
    matchLabels:
      app: gateway-api
  template:
    metadata:
      labels:
        app: gateway-api
    spec:
      imagePullSecrets:
        - name: harbor-secret
      containers:
        - name: gateway-api-container
          image: "{{- .Values.harbor.secret.host -}}/{{- .Values.gatewayApi.image.repository -}}:{{- .Values.gatewayApi.image.tag -}}"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: {{- .Values.gatewayApi.env.appPort -}}
          env:
            - name: APP_PORT
              value: "{{- .Values.gatewayApi.env.appPort -}}"
            - name: DB_HOST
              value: db-service
            - name: DB_PORT
              value: "5432"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: POSTGRES_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: POSTGRES_PASSWORD
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: POSTGRES_DB
