{{- if .Values.gatewayApi.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: gateway-api-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: gateway-api-deployment
  minReplicas: {{ .Values.gatewayApi.hpa.minReplicas }}
  maxReplicas: {{ .Values.gatewayApi.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.gatewayApi.hpa.targetCpuUtilization }}
{{- end }}
